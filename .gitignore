# Built artifacts
/.gradle/
/build/
/app/build/
/local.properties
*.iml
.idea/
.DS_Store
/captures/
*.apk
*.aab
*.ap_
*.dex
.kotlin/
*.log
*.hprof
.env
.env.*
secrets.properties

# Firebase — real client config (API key / project ids). Keep the .example only.
/app/google-services.json
google-services.json
!**/google-services.json.example

# Google / Firebase service accounts & similar (App Distribution, CI)
*service-account*.json
*-firebase-adminsdk*.json
firebase-adminsdk*.json

# Release signing — never commit (cannot rebuild signed APK without these)
/keystore.properties
/keystore/
*.jks
*.keystore

# --- Private IP: keep on this machine only (repo must not be enough to clone the product) ---
# USSD dial / accessibility automation / PIN overlay / dialer adapters
/app/src/main/java/com/lipabill/app/ussd/
/app/src/main/res/xml/ussd_accessibility_service.xml
# M-Pesa SMS → transaction parsing (core ingest logic)
/app/src/main/java/com/lipabill/app/data/parser/
# At-rest seal used for merchant backup blob
/app/src/main/java/com/lipabill/app/security/AppAtRestCrypto.kt

# Brag / launch-video artifacts
brag-output/
*brag*

# Markdown — keep README only
*.md
!README.md

# R8 mapping (local only — never ship)
/app/build/outputs/mapping/

# Local design / review media (large; not needed to build)
/artwork/

# Tests for private parser IP
/app/src/test/java/com/lipabill/app/data/parser/

# Local install helpers (references private USSD service)
/tools/
