<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- App is local-only; deny cleartext if anything is added later. -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
